name: First Test Workflow
on: workflow_dispatch
jobs:
  first-job:
   runs-on: ubuntu-latest
   steps:
     - name: Print greeting
       run: 
          echo "hello test workflow"
          echo "he fetquest"
     - name: Print goodbye
       run: echo "goodbye workflow"
     
